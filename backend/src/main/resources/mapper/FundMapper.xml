<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fund.mapper.FundMapper">
    
    <!-- 查询所有基金 -->
    <select id="selectAll" resultType="com.fund.entity.Fund">
        SELECT id, fund_code AS fundCode, fund_name AS fundName, create_time AS createTime
        FROM fund
        ORDER BY create_time DESC
    </select>
    
    <!-- 根据基金代码查询 -->
    <select id="selectByCode" resultType="com.fund.entity.Fund">
        SELECT id, fund_code AS fundCode, fund_name AS fundName, create_time AS createTime
        FROM fund
        WHERE fund_code = #{fundCode}
    </select>
    
    <!-- 插入基金 -->
    <insert id="insert" parameterType="com.fund.entity.Fund" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO fund (fund_code, fund_name, create_time)
        VALUES (#{fundCode}, #{fundName}, NOW())
    </insert>
    
    <!-- 删除基金 -->
    <delete id="deleteByCode">
        DELETE FROM fund WHERE fund_code = #{fundCode}
    </delete>
    
    <!-- 检查基金是否存在 -->
    <select id="countByCode" resultType="int">
        SELECT COUNT(*) FROM fund WHERE fund_code = #{fundCode}
    </select>
    
</mapper>